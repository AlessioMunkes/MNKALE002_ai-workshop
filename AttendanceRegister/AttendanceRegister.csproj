<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>AttendanceRegister</RootNamespace>
    <AssemblyName>AttendanceRegister</AssemblyName>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="ClosedXML" Version="0.104.1" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="8.0.8" />
    <!-- Pure managed QR generation. No System.Drawing, so it runs the same on
         Windows, macOS and Linux, and emits SVG we can inline directly. -->
    <PackageReference Include="QRCoder" Version="1.6.0" />
  </ItemGroup>

  <ItemGroup>
    <None Update="App_Data\**\*.*" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
